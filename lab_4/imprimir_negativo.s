main:
	addi t0, zero, 4
	ecall

	addi t0, zero, 1
	ecall
	ret

	#doido q está funcionando. Why??

	#ent aparentemente funciona se eu escrever e 
	#ler no mesmo registrador, 
	#mas ao fazer uma operacao nao da certo